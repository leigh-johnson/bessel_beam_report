% ============================================================
%  Pi2 Final Report — PRL-style (REVTeX 4.2)
%  Target length: ~5 pages in PRL format
%
%  Compile:  latexmk -pdf main.tex   (or use the helper: `./build.sh`)
%  Overleaf: upload this folder as a project; it compiles as-is.
%
%  Submission: export the PDF and name it
%      Zaman_Leigh_Pi2_Report.pdf
%  (`./build.sh submission` does this automatically)
% ============================================================
\documentclass[
  aps,
  % prl,
  twocolumn,
  reprint,              % journal-style layout; use `preprint` for single-column draft
  superscriptaddress,
  amsmath,
  amssymb,
  floatfix,
  longbibliography,      % show article titles in the bibliography
  nofootinbib % do not move footnotes to bibliography
]{revtex4-2}
\usepackage{style} % custom style file for Pi2 final report
\begin{document}

% ============================================================
%  TITLE BLOCK
%  Guide asks for: Project Title / Scholar name / Mentor name /
%  Professor lab
% ============================================================
\title{Project Title}   % TODO: project title

\author{Leigh Zaman}
\email{leighzaman@berkeley.edu}   % optional; delete if not wanted
\affiliation{Physics Innovators Initiative (Pi$^2$), Department of Physics, University of California, Berkeley, CA}

\author{Anke Stoeltzel}
\thanks{Graduate Mentor}
\affiliation{Kolkowitz Group, Department of Physics, University of California, Berkeley, CA}

\date{\today}

% ============================================================
%  ABSTRACT (comes BEFORE \maketitle in REVTeX)
% ============================================================
\begin{abstract}
  TODO: abstract context, what was done, key quantitative result, significance.
\end{abstract}

\maketitle

% \tableofcontents
% ============================================================
%  I. BACKGROUND AND AIMS
%  - Background and motivation of the project
%  - Major questions to address
%  - Hypothesis in addressing the question
% ============================================================
\section{Background and Aims}
Optical lattice clocks are precise enough to resolve the gravitational
redshift at tabletop scale. The first-generation strontium clock in our
lab resolved the redshift across five atom ensembles spanning a
\SI{1}{\centi\meter} vertical path
\cite{zhengLabbasedTestGravitational2023}. The second-generation clock
extends this to three ensembles across \SI{1}{\meter}. A vertical
optical lattice traps the strontium atoms, and a co-propagating
\SI{698}{\nano\meter} clock laser interrogates (probes) them. Since the
redshift is proportional to height, intensity variations along the
interrogation path introduce a systematic error that must be
characterized. The clock beam would ideally be as uniform as possible across over a meter of propagation, which is not possible with a conventional focused Gaussian beam (see \autoref{fig:theory_gaussian_vs_bessel_intro}). A Bessel
beam is a promising candidate because it has a non-diffracting profile over long propagation distances~\cite{durninDiffractionfreeBeams1987,
  durninComparisonBesselGaussian1988, raoConceptualReviewBessel2024}.

This study addresses one primary question: can a three-axicon system built from 1~in. optics produce a Bessel zone that meets the following requirements for clock interrogation:
\begin{enumerate}
  \item Bessel zone length of at least \SI{1}{\meter}.
  \item Central lobe of the Bessel region is large enough to contain a cloud of strontium atoms (cross-sectional FWHM at least \SI{300}{\micro\meter} in diameter).
  \item Uniform on-axis intensity profile.
  \item Bessel region fits within 1/2~in.\ mirror (mirror 1, entrance to science chamber).
  \item Annulus after Bessel region is larger than 1/2~in.\ (minimize reflections off mirror 2 in the science chamber).
\end{enumerate}
We tested this by building four candidate configurations and comparing the measured Bessel zone of each against models described in \autoref{sec:geometric_model} and \autoref{sec:qdht_model}.

\begin{figure}[h]
  \includegraphics[width=\linewidth]{gaussian_vs_bessel_intro}
  \caption{\label{fig:theory_gaussian_vs_bessel_intro} Comparison of an ideal Gaussian beam and zeroth-order Bessel beam. (a)~Transverse intensity profile of a Gaussian beam at its waist. (b)~Transverse intensity profile $I(r) \propto J_0^2(k_r \cdot r)$ of an ideal Bessel beam, showing the central lobe and surrounding concentric rings. (c)~Longitudinal intensity profile of a Gaussian beam, showing how the beam's width grows (diverges) as it propagates. (d)~The Bessel beam's central lobe stays constant/uniform. }
\end{figure}

% TODO: Background on Gaussian vs. Bessel Beam, Durnin \cite{durninComparisonBesselGaussian1988}

% TODO: Background on Geometric and Wave Optics Models, Rao

% TODO: Background on Hollow Gaussian Beam (annulus) quality and influece on on-axis intensity modulations/ripples, Rao

% ============================================================
%  II. EXPERIMENTAL/THEORETICAL APPROACHES
%  - Describe in detail the experimental and/or theoretical
%    approaches of the project
% ============================================================
\section{Experimental and Theoretical Approaches}
\begin{figure*}[ht]
  \includegraphics[width=\textwidth]{geometric_model}
  \caption{\label{fig:theory_geometric_model} Geometric model of the three-axicon Bessel beam system, shown for candidate \#2 (\autoref{tab:configs}). Axicon~1 ($\alpha_1=\SI{5}{\degree}$) converts the Gaussian input beam into a cone of rays deflected by $\beta_1$. Axicon~2 ($\alpha_2=\SI{5}{\degree}$) collimates them into an annulus of inner radius $\rho_{\text{in}}$ and outer radius $\rho_{\text{out}}$. Axicon~3 ($\alpha_3=\SI{0.5}{\degree}$) deflects the annulus rays by $\theta$. The region where the rays overlay on axis (red) is the geometric Bessel range (shown here as \SI{1.18}{\meter}). $L_{12}$ and $L_{23}$ are the axicon separations. The first 1/2~in.\ mirror steers the beam into the science chamber, and the second 1/2~in.\ mirror must be placed in the void zone to minimize stray reflections.}
\end{figure*}


\subsection{Bessel Beams and Bessel-Gauss Beams}

On the plane transverse to the propagation direction, an ideal Bessel beam (BB) has an invariant intensity distribution described by a Bessel function of the first kind, $J_0$. This property led to the description of BBs as non-diffracting beams, since the transverse intensity profile does not change as the beam propagates through space~\cite{durninDiffractionfreeBeams1987}. The ideal Bessel beam is non-diffracting over a theoretically infinite range. The transverse intensity distribution of an ideal Bessel beam has the form:
\begin{equation}
  I(r) \propto J_0^2(k_r \cdot r),
  \label{eq:ideal_bessel_beam}
\end{equation}
where $k_r$ is the transverse wavevector satisfying $k_r^2 + k_z^2 = k^2 = (2\pi/\lambda)^2$ and $r$ is the radial distance from the propagation axis.

Passing a Gaussian beam through an axicon (another name for a conical prism) produces a Bessel-Gauss beam by converting the Gaussian input into a conical wavefront that interferes constructively along the propagation axis \cite{raoConceptualReviewBessel2024}. An axicon of base angle $\alpha$ and refractive index $n$ deflects the Gaussian beam toward the axis by $\beta = \arcsin(n \sin \alpha) - \alpha$, creating a Bessel-Gauss beam of the zeroth order. The Bessel-Gauss beam has a transverse intensity profile described by \autoref{eq:ideal_bessel_beam}, where $k_r = (2\pi/\lambda)\sin\beta$ is governed by the axicon deflection angle $\beta$ and the wavelength $\lambda$ of the input Gaussian beam.

\subsection{Geometric / Ray Model}\label{sec:geometric_model}
\begin{figure}[ht]
  \begin{tikzpicture}
    \node[anchor=south west,inner sep=0] (a)
    {\includegraphics[width=\linewidth]{theory_three_axicon_L12_between_axicon12.png}};
    \node[anchor=south west,font=\bfseries,inner sep=2pt]
    at ([yshift=1pt]a.north west) {(a)};
  \end{tikzpicture}
  \begin{tikzpicture}
    \node[anchor=south west,inner sep=0] (b)
    {\includegraphics[width=\linewidth]{theory_three_axicon_L12.png}};
    \node[anchor=south west,font=\bfseries,inner sep=2pt]
    at ([yshift=1pt]b.north west) {(b)};
  \end{tikzpicture}
  \caption{\label{fig:theory_three_axicon_L12} Effect of axicon~1-2 separation $L_{12}$ in optics configuration \#1 (see \autoref{tab:configs}), simulated with QDHT. (a)~Propagation between axicons~1 and~2: after axicon~1 Bessel zone (shaded in grey), the beam becomes an expanding annulus with approximately \SI{2}{\milli\meter} FWHM thickness (shaded orange region). $L_{12}$ sets the annulus radius arriving at axicon~2 (annulus radius shown on right y-axis). The 85-90\% aperture limit of 1~in.\ optics (black dotted line) determines the upper limit on $L_{12}$ separation. (b)~Comparison of on-axis intensity after axicon~3 for $L_{12} = 140$, 190, 240, \SI{290}{\milli\meter}. Increasing the $L_{12}$ separation suppresses the on-axis intensity ripples.}
\end{figure}

We based the design on the collimating three-axicon zoom system described in \citet{schniederCollimatingThreeaxiconZoom2024}. A geometric model of our three-axicon system is shown in \autoref{fig:theory_geometric_model}. Axicon~1 converts the Gaussian input beam into a cone of rays deflected by $\beta_1$. Axicon~2 collimates them into an annulus of inner radius $\rho_{\text{in}}$ and outer radius $\rho_{\text{out}}$. Axicon~3 deflects the annulus rays by $\theta$. The region where the rays overlay on axis (red) is the geometric Bessel range. $L_{12}$ and $L_{23}$ are the axicon separations. Because the annulus leaving axicon~1 keeps expanding as it propagates, the 1~in.\ aperture of axicon~2 sets an upper limit on $L_{12}$; within that limit, increasing $L_{12}$ suppresses the on-axis intensity ripple (\autoref{fig:theory_three_axicon_L12}).

\subsection{Wave Optics / QDHT Model}\label{sec:qdht_model}

The geometric model predicts where rays overlap, but not the intensity within the Bessel zone. To get a sense of the idealized intensity distribution, we constructed a wave-optics simulation. We first attempted the analysis in Zemax software, whose physical optics propagation uses a fast Fourier transform (FFT) on a square Cartesian grid~\cite{ZemaxOpticStudio}. We discarded this approach out of concern that design decisions would be governed by non-physical artifacts of the square sampling grid.

Since the idealized optical system is rotationally symmetric about the propagation axis, we were able to model the field propagation with a quasi-discrete Hankel transform (also known as a Fourier-Bessel transform) described by \citet{yuQuasidiscreteHankelTransform1998a}.

Our QDHT model implementation accepts only measured quantities as inputs: the Gaussian input beam waist $w_0$, the axicon base angles $\alpha_1$, $\alpha_2$, and $\alpha_3$, the axicon separations $L_{12}$ and $L_{23}$, and the wavelength $\lambda$. The model has no free parameters fit to our data. The QDHT models in \autoref{fig:measured_window_envelopes_lobe_diam_colored} are sampled at $N=4096$ points in the radial direction over a domain padded to $2.5\times$ the Gaussian input beam radius.

We implemented both geometric and QDHT models in an open-source Python simulator with an interactive web interface\footnotemark[1]. Parameter sweeps over the axicon angles, separations, and input beam diameter are collected in \autoref{sec:model_parameter_sweeps}.

\footnotetext[1]{\url{https://bessel-beam.streamlit.app/}}

\subsection{Experimental Setup}\label{sec:experimental_setup}
\begin{table*}[!t]
  \caption{Optics configuration candidates. All configurations use
  1~in.\ (\SI{25.4}{\milli\meter}) axicons with base angles
  $\alpha_{1}=\alpha_{2}=\SI{5}{\degree}$ and
  $\alpha_{3}=\SI{0.5}{\degree}$. $L_{12}$ and $L_{23}$ are the
  axicon~1--2 and axicon~2--3 separations, $D_{\text{in}}$ is the
  $1/e^{2}$ diameter of the Gaussian input beam at axicon~1,
  $(\Delta x,\Delta y,\Delta z)$ are the translation-stage step sizes
  used to acquire each dataset, and
  $\lambda=\SI{650}{\nano\meter}$ is the wavelength of the input
  laser diode.
  \label{tab:configs}}
  \begin{ruledtabular}
    \begin{tabular}{c>{\raggedright\arraybackslash}p{0.26\textwidth}cccccc}
      \# & Description                                                 & $L_{12}$ (mm) & $L_{23}$ (mm) & $D_{\text{in}}$ (mm)        & $2\times$ telescope & $(\Delta x,\Delta y,\Delta z)$ (mm) & Dataset (\# of images) \\
      \colrule
      1  & Baseline
         & 190                                                         & \phantom{0}50 & 4.59          & ---                         & ?, ?, ?             & Jul 22--23 (1,202)                                           \\
      2  & Gaussian input beam magnified $2\times$ vs.\ baseline (\#1)
         & 240                                                         & 220           & 9.44          & input\footnotemark[1]       & ?, ?, ?             & Jul 29--30 (1,013)                                           \\
      3  & Hollow Gaussian beam diameter reduced vs.\ baseline (\#1)
         & 175                                                         & 415           & 4.59          & ---                         & ?, ?, ?             & Aug 6 (411)                                                  \\
      4  & Bessel zone magnified $2\times$ vs.\ \#3
         & 175                                                         & 415           & 4.59          & Bessel zone\footnotemark[2] & ?, ?, ?             & Aug 6 (241)                                                  \\
    \end{tabular}
  \end{ruledtabular}
  \footnotetext[1]{$2\times$ magnification telescope before axicon~1,
    expanding the Gaussian input beam. Telescope constructed with 1 in. lenses: $f_1 = \SI{-50}{\milli\meter}$ (plano-concave), $f_2 = \SI{+100}{\milli\meter}$ (plano-convex) mounted in a 1 in. optical cage.}
  \footnotetext[2]{$2\times$ magnification telescope placed within the
    Bessel zone at $z = 120$ cm after axicon~3. Telescope constructed with 2 in. lenses: $f_1 = \SI{+100}{\milli\meter}$, $f_2 = \SI{+200}{\milli\meter}$, mounted directly to the optics breadboard table.}
\end{table*}

We coupled an adjustable-focus laser diode (Thorlabs CPS650F, nominal
$\lambda = \SI{650}{\nano\meter}$) to a single-mode fiber with a fiber
optic coupler (Schafter+Kirchhoff 60FC-L-4-M25-24), producing a Gaussian
input beam with $1/e^2$ diameter $D_{\text{in}} = \SI{4.59}{\milli\meter}$
at axicon~1 and \SI{1.2}{\milli\watt} of power after the coupler.

We mounted three UV fused silica axicons (Thorlabs AX255-A and AX2505-A;
$n \approx 1.4565$ at \SI{650}{\nano\meter}, AR-coated for
350--\SI{700}{\nano\meter}; $\alpha_1 = \alpha_2 = \SI{5}{\degree}$,
$\alpha_3 = \SI{0.5}{\degree}$) in XY translation mounts and measured the
separations $L_{12}$ and $L_{23}$ with a stainless steel ruler. We
aligned the system by centering the annulus on each successive optic
(``beam walking''), using the beam profiler gantry as the alignment
tool. The four candidate configurations we evaluated are listed in
\autoref{tab:configs}.

We characterized the Bessel zone by imaging the beam through an OD=4.0
filter directly onto a monochrome CMOS sensor (FLIR BFS-PGE-31S4M-C,
$2048 \times 1536$ pixels, \SI{3.45}{\micro\meter} pixel size) mounted
on a motorized three-axis CNC gantry built for this project, which allowed us to sample the beam profile along \SI{2}{\meter} of propagation with repeatable sub-millimeter positioning. The gantry has \SI{130}{\milli\meter} of travel along the propagation direction and \SI{100}{\milli\meter} of travel in the transverse XY plane. Because the $z$ travel is shorter than the region of interest, we repositioned the gantry by hand along the optical table to cover the full \SI{2}{\meter} propagation path. Each placement is registered against a sheet of \SI{1}{\centi\meter} graph paper fixed to the table (\autoref{fig:gantry_isometric_photo}), so the measurement position is repeatable between runs. At each plane the acquisition software locates the beam and sets the exposure time automatically, so that the brightest pixel in the plane falls below a saturation threshold. It also collects dark frames for background subtraction. If the beam diameter is larger than the camera sensor, the profiler software collects a grid of images and stitches them into a single composite image.

\begin{figure}[ht]
  \includegraphics[width=\linewidth]{gantry_isometric_photo}
  \caption{\label{fig:gantry_isometric_photo} Isometric view of the
    motorized three-axis CNC gantry built for this project. The
    monochrome CMOS sensor (FLIR BFS-PGE-31S4M-C) rides on the moving
    stage and images the beam directly through an OD${=}4.0$ filter. The
    gantry provides \SI{130}{\milli\meter} of travel along the
    propagation direction and \SI{100}{\milli\meter} in the transverse XY
    plane; to reach the full \SI{2}{\meter} path it is moved by hand along
    the table and registered against the \SI{1}{\centi\meter} graph paper
    fixed beneath it.}
\end{figure}

\subsection{Metrics}\label{sec:metrics}
From each plane, we crop the image to the central Bessel lobe. We report intensity as pixel counts / exposure time, so planes recorded with different exposures are comparable. We extract the on-axis intensity $I(r=0, z)$ by fitting a $J_0^2$ function to the data (see \autoref{fig:placement_cmp_report_jul29}), where the amplitude $A$ represents the peak intensity of the plane. From the fitted $k_r$ wavenumber, we calculate the central lobe diameter $\varnothing$ and mean radial FWHM $\langle \mathrm{FWHM} \rangle_r$. The measured Bessel zone $Z_R$ is the interval between the outermost points at which $I(r=0, z)$ falls to $50\%$ of its maximum, and $\Delta Z_R$ is the length of that interval. The ripple metric is the peak-to-peak variation of $I(r=0, z)$ within the measured Bessel zone, normalized to the mean on-axis intensity.

The scan control software, analysis notebooks, and mechanical designs are open source\footnotemark[2].

\footnotetext[2]{\url{https://github.com/leigh-johnson/bessel_beam_profiler}}

% ============================================================
%  III. EXPERIMENTAL/THEORETICAL RESULTS
%  - Descriptions/figures/tables of findings
%  - New techniques and methods developed
%  - How the research addresses the posed questions
% ============================================================

\section{Results}\label{sec:results}
None of the four configurations using 1 in. axicons described in \autoref{tab:configs} met all of the design requirements.

The measured Bessel zone for each configuration is summarized in \autoref{tab:results_summary} and \autoref{fig:measured_window_envelopes_lobe_diam_colored}, along with the modeled geometric and QDHT ranges. We evaluated four candidates, described in \autoref{tab:configs}. The QDHT simulation consistently predicted the starting point of the Bessel range for three candidates within $10\%$ agreement. In two of the three modeled candidates, the Bessel zone length was within $8\%$ agreement with the QDHT simulation. The geometric model consistently predicted the start of the Bessel zone within $3.6\%$ to $5.6\%$ agreement but overestimated the Bessel zone length by $+42\%$ to $+89\%$.

\begin{figure}[bh]
  \includegraphics[width=\linewidth]{placement_cmp_report_jul29}
  \caption{\label{fig:placement_cmp_report_jul29} Data from the Bessel zone of optics configuration \#2 in \autoref{tab:configs}. (a) Cross-section image of beam profile at $z=$ \SI{175}{\centi\meter}. Image data is cropped to $\pm$ \SI{200}{\micro\meter} of the central Bessel lobe, colored by intensity (pixel counts / exposure time) and normalized.  (b) Measured intensity sampled on a 2x2 pixel grid (6.9 x 6.9 \si{\micro\meter}) and fit to a $J_0^2$ function (black line) to extract the core diameter and FWHM. (c) $z =$ \SI{180}{\centi\meter} cross-section, intensity normalized against the peak intensity measurement at $z=$ \SI{175}{\centi\meter} shown in (a). (d) $J_0^2$ fit to the $z =$ \SI{180}{\centi\meter} cross-section.}
\end{figure}

% RESULT TABLE V2
% measured / QDHT / geometric in separate rows to resolve column overflow in V1 table
\begin{table*}[!t]
  \caption{Measured and modeled Bessel zone for the optics configuration
    candidates described in \autoref{tab:configs}. All $z$ values are
    measured along the propagation direction from the face of axicon~3.
    $Z_R$ is the range of the Bessel region and $\Delta Z_R$ its length
    ($\pm$ imaging system $z$-step size). The mean core diameter $\varnothing$ and core
    FWHM are averages of the per-plane $J_0^2$ fits within the measured
    Bessel zone (deviation from ideal value in parentheses). The geometric
    range is calculated by the method described in \autoref{sec:geometric_model}; the QDHT range is
    the FWHM of the modeled on-axis intensity $I(0,z)$ described in \autoref{sec:qdht_model}.
    Parenthetical percentages on model rows give the deviation from the
    measured value, and are upper limits where the measurement is a lower
    bound. No simulation/model was developed for configuration \#4. Ripple (\%) is the ratio of the peak-to-peak variation in the on-axis intensity $I(0,z)$ to the mean on-axis intensity within the measured Bessel zone. Peak intensity (counts/\si{\micro\second}) is the maximum measured on-axis intensity within the measured Bessel zone, used to normalize the measured on-axis intensity in \autoref{fig:measured_window_envelopes_lobe_diam_colored}.
    \label{tab:results_summary}}
  \begin{ruledtabular}
    \begin{tabular}{llcccccc}
      Config & Method    & $Z_R$ (m)                               & $\Delta Z_R$ (m)    & Core $\varnothing$ (\si{\micro\meter}) & $\langle \mathrm{FWHM} \rangle_r$ (\si{\micro\meter}) & Ripple (\%) & Peak (counts/\si{\micro\second}) \\
      \colrule
      \#1    & measured  & $[1.29, 1.68]$   $\pm$ 0.01             & 0.38 $\pm$ 0.01     & 126.4 ($+1.7\%$)                       & 59.2                                                  & 48          & 0.723                            \\
             & geometric & $[1.34\,(+3.6\%),\ 1.91\,(+13\%)]$      & 0.57 ($+49\%$)      &                                        &                                                       &             &                                  \\
             & QDHT      & $[1.40\,(+8.2\%),\ 1.77\,(+4.9\%)]$     & 0.37 ($-3\%$)       &                                        &                                                       &             &                                  \\ \hline
      \#2\footnotemark[1]
             & measured  & $[1.18, {>}2.01]$                       & ${>}0.83$           & 118.2 ($-5.0\%$)                       & 55.4                                                  & 63          & 0.590                            \\
             & geometric & $[1.23\,(+4.2\%),\ 2.41\,({<}{+}20\%)]$ & 1.18 (${<}{+}42\%$) &                                        &                                                       &             &                                  \\
             & QDHT      & $[1.29\,(+9.3\%),\ 2.19\,({<}{+}9\%)]$  & 0.90 (${<}{+}8\%$)  &                                        &                                                       &             &                                  \\ \hline
      \#3\footnotemark[2]
             & measured  & $[1.12, 1.42]$   $\pm$ 0.01             & 0.30 $\pm$ 0.01     & 125.0 ($+0.5\%$)                       & 58.5                                                  & 63          & 2.003                            \\
             & geometric & $[1.18\,(+5.6\%),\ 1.76\,(+24\%)]$      & 0.57 ($+89\%$)      &                                        &                                                       &             &                                  \\
             & QDHT      & $[1.21\,(+8.3\%),\ 1.62\,(+14\%)]$      & 0.41 ($+36\%$)      &                                        &                                                       &             &                                  \\ \hline
      \#4\footnotemark[3]
             & measured  & $[0.47, 0.79]$ $\pm$ 0.01               & 0.32 $\pm$ 0.01     & 268.1 ($+7.8\%$)                       & 125.6                                                 & 50          & 0.323                            \\
    \end{tabular}
  \end{ruledtabular}
  \footnotetext[1]{Experimental setup did not allow for measurement of the
    Bessel zone beyond \SI{2.0}{\meter}. The measured $Z_R$ and $\Delta Z_R$ should be interpreted as lower bounds. See \autoref{sec:future_work} for discussion of how to extend the measurement range.}
  \footnotetext[2]{Alignment of axicons~2 and 3 was sub-optimal, resulting
    in a reduced $\Delta Z_R$. See \autoref{fig:annulus_composites} for a comparison of candidate $\#1$ and $\#3$ alignment.}
  \footnotetext[3]{A limited dataset was collected for configuration \#4.
    The $z$ axis measurements are taken after the telescope output lens
    instead of after axicon~3.}
\end{table*}

\begin{figure*}[bh]
  \includegraphics[width=\textwidth]{measured_window_envelopes_lobe_diam_colored}
  \caption{\label{fig:measured_window_envelopes_lobe_diam_colored} Measured on-axis intensity $I(0,z)$ along the propagation direction for candidates (a)~\#1, (b)~\#2, (c)~\#3, and (d)~\#4 in \autoref{tab:configs}. Each panel normalized to its own peak intensity rate (counts/\si{\micro\second}). $z$ is measured from the last optical element. Points are $J_0^2$ amplitudes fit to cropped cross-section images of the plane transverse to the propagation direction. Points are colored by the deviation of the fitted central/core lobe diameter from the ideal value. The solid black line is the QDHT model, dashed red line indicates 50\% of the measured peak, and the grey shaded region is the measured Bessel zone $Z_R$ between the outermost 50\% crossings. The ripple metric is the peak-to-peak variation of the on-axis intensity within the measured Bessel zone. Candidate \#2's Bessel zone was not fully characterized due to space limitations on the optics table, so the measured $Z_R$ and $\Delta Z_R$ should be interpreted as lower bounds. No QDHT model was developed for candidate \#4, which placed a $2\times$ magnification telescope inside the Bessel zone.}
\end{figure*}


Candidate \#1 was the baseline configuration. We measured Bessel zone length of $\Delta Z_R = 0.38 \pm 0.01$ m, within $\approx$\SI{1}{\centi\meter} agreement with the length predicted by the QDHT model. The measured Bessel zone range $Z_R \in [1.29, 1.68]$ m was $\approx$10 cm from the predicted QDHT range. The mean measured core diameter and FWHM were within 2\% of the ideal $J_0^2$ values at all points measured within the Bessel zone, with a mean core diameter of \SI{126.4}{\micro\meter} and FWHM of \SI{59.2}{\micro\meter}.

Candidate \#2 used a $2\times$ magnification telescope before axicon~1 to expand the Gaussian input beam. Expanding the input beam substantially increased the Bessel zone length ($\Delta Z_R > 0.83$ m), but at the cost of worse on-axis intensity ripples.
Space limitations on the optics table prevented full characterization of the Bessel zone, so the measured $Z_R$ and $\Delta Z_R$ should be interpreted as lower bounds. The spacing between axicon~1 and axicon~2 ($L_{12} =$ \SI{240}{\milli\meter}) was the maximum allowed by the 1~in.\ aperture of axicon~2. The measured Bessel zone range $Z_R \in [1.18, >2.01]$ m was $\approx$ 10 cm from the predicted QDHT range. The mean measured core diameter and FWHM were within 5\% of the ideal $J_0^2$ values at all points measured within the Bessel zone, with a mean core diameter of \SI{118.2}{\micro\meter} and FWHM of \SI{55.4}{\micro\meter}.

Candidate \#3 repeated the setup of Candidate \#1, with the distance between axicons modified ($L_{12} = \SI{175}{\milli\meter}$ vs. $L_{12} = \SI{190}{\milli\meter}$ and $L_{23} = \SI{415}{\milli\meter}$ vs. $L_{23} = \SI{50}{\milli\meter}$), resulting in a measured Bessel zone length of $\Delta Z_R = 0.30 \pm 0.01$ m. The measured Bessel zone range $Z_R \in [1.12, 1.42]$ m was $\approx$ 8 cm from the predicted QDHT range start point. The mean measured core diameter and FWHM were within 1\% of the ideal $J_0^2$ values at all points measured within the Bessel zone, with a mean core diameter of \SI{125.0}{\micro\meter} and FWHM of \SI{58.5}{\micro\meter}. Notably, the alignment of axicon~2 and axicon~3 was sub-optimal, resulting in a reduced $\Delta Z_R$ (see \autoref{fig:annulus_composites} for a comparison of candidate $\#1$ and $\#3$ alignment).

Candidate \#4 repeated the setup of Candidate \#3, with a $2\times$ magnification telescope placed within the Bessel zone at $z = \SI{120}{\centi\meter}$ after axicon~3. The telescope doubled the transverse core size as expected, but,
surprisingly, left the Bessel zone length nearly unchanged
($\Delta Z_R = 0.32 \pm 0.01$~m vs.\ $0.30 \pm 0.01$~m for \#3) rather
than stretching it by the expected longitudinal magnification. We
examine this result in Sec.~\ref{sec:telescope}.

\subsection{Ripples in On-Axis Intensity}
The ripple \% metric is defined in \autoref{sec:metrics}. The on-axis intensity ripples varied by 48-63\% of the mean within every candidate's Bessel zone (see \autoref{fig:measured_window_envelopes_lobe_diam_colored}), which ruled out all four under the uniformity requirement.

In early design/modeling stages, we were unsure if the on-axis intensity ripples were a numerical artifact of the simulation sampling/aliasing. However, the ripples persisted across a wide range of sampling parameters. Initial measurements with a manual translation stage (see figure TODO) were too coarse to resolve them, but finer scans with the motorized gantry revealed on-axis intensity modulations in agreement with those predicted analytically by Rao~\cite{raoOnaxisIntensityModulationfree2018} and reproduced by the QDHT simulations.

\subsection{Axicon Alignment Drift}
To test repeatability, we re-measured three $z$ positions of candidate
\#2's Bessel zone five days after the original scan
(\autoref{fig:placement_cmp_report_jul29} vs. \autoref{fig:placement_cmp_report_aug3}). The intensity ripples were reproduced. The fitted core diameters agreed within a few percent, but the peak intensity dropped from 0.48 to 0.33 counts/\si{\micro\second}, suggesting the drift did not occur uniformly. In \autoref{fig:placement_cmp_report_aug3}, the X-chord (blue dots) shifted more than the Y-chord (orange dots), resulting in a slight astigmatism. Fixing it required systematically adjusting the horizontal axes of all XY translation mounts to locate the source.

\begin{figure}[bh]
  \includegraphics[width=\linewidth]{placement_cmp_report_aug3}
  \caption{\label{fig:placement_cmp_report_aug3} TODO caption about alignment drift.}
\end{figure}

\subsection{Effect of M=2x Telescope}\label{sec:telescope}
Candidate \#4 placed a $2\times$ magnification telescope inside the
Bessel zone, $z = \SI{120}{\centi\meter}$ after axicon~3
(\autoref{tab:configs}), to understand the effect of magnification on the Bessel beam. The mean core diameter of \SI{268.1}{\micro\meter} is within $7.8\%$ of
twice the ideal core size, confirming the expected $2\times$ transverse
magnification in the XY plane. This behavior was similar to the effect on a Gaussian beam, where an $M=2$ telescope doubles the waist of the Gaussian beam.

The peak intensity dropped to 0.32 counts/\si{\micro\second},
compared to 2.00 for the identical optics without the telescope
(candidate \#3). Spreading the same power over a $4\times$ larger area ($M^2 = 4$) alone would predict 0.50 counts/\si{\micro\second}. The remaining loss would only be consistent with $\approx$0.7 counts/\si{\micro\second} ($\approx$35\% of the incident power) lost at the telescope lenses, so the coating and alignment of this configuration should be re-checked. However, the measured Bessel zone length was $\Delta Z_R = 0.32 \pm 0.01$ m, only slightly longer than the $\Delta Z_R = 0.30 \pm 0.01$ m measured for candidate \#3 without the telescope. The measured length contradicts the expected $2\times$ longitudinal magnification of the Bessel zone. However, gaps in the dataset at $z \approx 0.2$m could explain this discrepancy if the missing data contains a ripple peak that would extend the measured Bessel zone length.

\begin{figure}[bh]
  \includegraphics[width=\linewidth]{annulus_composites}
  \caption{\label{fig:annulus_composites} TODO caption about axicon 1-2 alignment, annulus quality, and on-axis intensity ripples.}
\end{figure}

% Example single-column figure (put files in figures/):
% \begin{figure}[tb]
%   \centering
%   \includegraphics[width=\columnwidth]{example.pdf}
%   \caption{Caption text. \label{fig:example}}
% \end{figure}
% Referenced as Fig.~\ref{fig:example}.

% Example two-column-spanning figure:
% \begin{figure*}[tb]
%   \centering
%   \includegraphics[width=\textwidth]{example_wide.pdf}
%   \caption{Wide caption text. \label{fig:wide}}
% \end{figure*}

% Example table:
% \begin{table}[tb]
%   \caption{Table caption. \label{tab:example}}
%   \begin{ruledtabular}
%     \begin{tabular}{lcc}
%       Quantity & Measured & Model \\
%       \colrule
%       $k_r$ & -- & -- \\
%     \end{tabular}
%   \end{ruledtabular}
% \end{table}

% ============================================================
%  IV. CONCLUSION AND PERSPECTIVE
%  - Major conclusion of the project
%  - Proposed future work, and why
% ============================================================
\section{Conclusion and Perspective}
We designed, built, and characterized a three-axicon Bessel beam
system and evaluated four candidates for the interrogation beam in a \SI{1}{\meter}
vertical strontium optical lattice clock. Every configuration we tested
produced a Bessel region with an identifiable core lobe in the cross-section. The measured core lobe diameters tracked
the ideal $J_0^2$ values to within 8\%. The QDHT wave-optics model predicted the start of the
Bessel zone within 10\% and, where we could measure it cleanly, the zone
length within 8\%.

However, none of the four 1~in.\ configurations met the design
requirements. The longest measured zone (${>}0.83$~m) fell short of the
\SI{1}{\meter} target, and the 48--63\% on-axis intensity ripple far
exceeded any acceptable uniformity threshold for clock interrogation.
Both limits trace back to the same constraint: the 1~in.\ aperture of the axicons caps the separation $L_{12}$. Larger separations would have delivered both a longer Bessel zone and better ripple suppression. Drifts in day-to-day alignment require additional study and characterization to understand the long-term stability of any final candidate. These results motivate the future work below, as well as the tooling we built to reduce the cost of every iteration that follows (automated beam profiler, QDHT simulator, axicon alignment tooling).


\subsection{Future Work}\label{sec:future_work}
Several open questions remain. First, we have not measured how the first $1/2$~in. steering mirror (mirror 1 in \autoref{fig:theory_geometric_model}) affects the beam profile. The mirror sits upstream of the Bessel zone, and diffraction from its edges could distort the structure of the beam before it reaches the atoms. Second, axicons with 2~in. diameter have been ordered to address the aperture limitations of 1~in. axicons. Third, day-to-day alignment drift limited the repeatability of our measurements, so the beam alignment should be monitored continuously. The automated alignment tool we built\footnotemark[3] could be run between measurements to detect drift and alert when remediation is required. The alignment tool could also be extended to drive motorized translation mounts, ensuring the axicon lenses are always aligned to a certain tolerance. Ultimately, the final system will interrogate strontium atoms at \SI{698}{\nano\meter} rather than the \SI{650}{\nano\meter} of our test laser diode. The selected configuration will likely need to be re-characterized with the clock laser, since the transverse wavenumber $k_r$ and Bessel zone geometry scale with wavelength.

\footnotetext[3]{\url{https://github.com/leigh-johnson/bessel_beam_profiler/blob/main/docs/axicon_alignment.md}}



% ============================================================
%  ACKNOWLEDGMENTS
% ============================================================
\begin{acknowledgments}
  Thank you to Anke Stoeltzel, Shuo Ma, Shimon Kolkowitz and the Kolkowitz Group for their support and guidance. This work was supported by the Pi$^2$ program and the UC Berkeley Physics Department.

  We acknowledge the use of Claude (Anthropic) for assistance with editing, literature review, Python code development, and LaTeX formatting. We take full responsibility for all scientific arguments, results, and conclusions presented in this report. This acknowledgment was inspired by Ref.~\cite{ceriottiEditorialPublishingPhysical2026}.
\end{acknowledgments}


% ============================================================
%  BIBLIOGRAPHY (entries live in references.bib)
% ============================================================
\bibliography{references}

\clearpage
% ============================================================
%  APPENDIX
% ============================================================
\appendix

\section{Model Parameter Sweeps}\label{sec:model_parameter_sweeps}
TODO: Pick a few figures that illustrate the effect of each level in the design: axicon angle, separation, input beam diameter.

TODO: explain single axicon sweep figs
\begin{figure}[ht]
  \includegraphics[width=\linewidth]{theory_single_axicon_angle_core.png}
  \caption{\label{fig:theory_single_axicon_angle_core} TODO caption about axicon angle + radial core diameter.}
\end{figure}
\begin{figure}[h]
  \includegraphics[width=\linewidth]{axicon_angle_sweep_D=4_59mm}
  \caption{\label{fig:theory_axicon_angle_sweep_D=4_59mm} TODO caption about axicon angle + bessel zone length.}
\end{figure}

\begin{figure}[h]
  \includegraphics[width=\linewidth]{axicon_angle_sweep_gain_D=4_59mm.png}
  \caption{\label{fig:theory_axicon_angle_sweep_gain_D=4_59mm} TODO caption about axicon angle + on-axis intensity gain.}
\end{figure}

\begin{figure}[h]
  \includegraphics[width=\linewidth]{theory_single_axicon_diameter.png}
  \caption{\label{fig:theory_single_axicon_diameter} TODO caption about input beam diameter + bessel zone length}
\end{figure}



\section{Axicon Alignment Tool}\label{sec:axicon_alignment_cli}

\begin{figure}[h]
  \includegraphics[width=\linewidth]{alignment_axicon2_before}
  \caption{\label{fig:alignment_axicon2_before} TODO caption }
\end{figure}

\begin{figure}[h]
  \includegraphics[width=\linewidth]{alignment_axicon2_after}
  \caption{\label{fig:alignment_axicon2_after} TODO caption }
\end{figure}



\end{document}
